/**
 * Game Aggregator API Documentation
 * Universal API for casino operators to access games from multiple providers through a single integration.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The StartAMultiSessionRequest model module.
 * @module model/StartAMultiSessionRequest
 * @version 1.0.0
 */
class StartAMultiSessionRequest {
    /**
     * Constructs a new <code>StartAMultiSessionRequest</code>.
     * @alias module:model/StartAMultiSessionRequest
     * @param playerId {String} Player's ID in your system.
     * @param currency {String} Three-letter currency code (ISO 4217).
     * @param countryCode {String} Two-letter country code (ISO 3166-1 alpha-2).
     * @param ipAddress {String} Player's IP address.
     */
    constructor(playerId, currency, countryCode, ipAddress) { 
        
        StartAMultiSessionRequest.initialize(this, playerId, currency, countryCode, ipAddress);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, playerId, currency, countryCode, ipAddress) { 
        obj['player_id'] = playerId;
        obj['currency'] = currency;
        obj['country_code'] = countryCode;
        obj['ip_address'] = ipAddress;
    }

    /**
     * Constructs a <code>StartAMultiSessionRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/StartAMultiSessionRequest} obj Optional instance to populate.
     * @return {module:model/StartAMultiSessionRequest} The populated <code>StartAMultiSessionRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new StartAMultiSessionRequest();

            if (data.hasOwnProperty('player_id')) {
                obj['player_id'] = ApiClient.convertToType(data['player_id'], 'String');
            }
            if (data.hasOwnProperty('currency')) {
                obj['currency'] = ApiClient.convertToType(data['currency'], 'String');
            }
            if (data.hasOwnProperty('country_code')) {
                obj['country_code'] = ApiClient.convertToType(data['country_code'], 'String');
            }
            if (data.hasOwnProperty('ip_address')) {
                obj['ip_address'] = ApiClient.convertToType(data['ip_address'], 'String');
            }
            if (data.hasOwnProperty('game_ids')) {
                obj['game_ids'] = ApiClient.convertToType(data['game_ids'], ['String']);
            }
            if (data.hasOwnProperty('locale')) {
                obj['locale'] = ApiClient.convertToType(data['locale'], 'String');
            }
            if (data.hasOwnProperty('device')) {
                obj['device'] = ApiClient.convertToType(data['device'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>StartAMultiSessionRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>StartAMultiSessionRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of StartAMultiSessionRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['player_id'] && !(typeof data['player_id'] === 'string' || data['player_id'] instanceof String)) {
            throw new Error("Expected the field `player_id` to be a primitive type in the JSON string but got " + data['player_id']);
        }
        // ensure the json data is a string
        if (data['currency'] && !(typeof data['currency'] === 'string' || data['currency'] instanceof String)) {
            throw new Error("Expected the field `currency` to be a primitive type in the JSON string but got " + data['currency']);
        }
        // ensure the json data is a string
        if (data['country_code'] && !(typeof data['country_code'] === 'string' || data['country_code'] instanceof String)) {
            throw new Error("Expected the field `country_code` to be a primitive type in the JSON string but got " + data['country_code']);
        }
        // ensure the json data is a string
        if (data['ip_address'] && !(typeof data['ip_address'] === 'string' || data['ip_address'] instanceof String)) {
            throw new Error("Expected the field `ip_address` to be a primitive type in the JSON string but got " + data['ip_address']);
        }
        // ensure the json data is an array
        if (!Array.isArray(data['game_ids'])) {
            throw new Error("Expected the field `game_ids` to be an array in the JSON data but got " + data['game_ids']);
        }
        // ensure the json data is a string
        if (data['locale'] && !(typeof data['locale'] === 'string' || data['locale'] instanceof String)) {
            throw new Error("Expected the field `locale` to be a primitive type in the JSON string but got " + data['locale']);
        }
        // ensure the json data is a string
        if (data['device'] && !(typeof data['device'] === 'string' || data['device'] instanceof String)) {
            throw new Error("Expected the field `device` to be a primitive type in the JSON string but got " + data['device']);
        }

        return true;
    }


}

StartAMultiSessionRequest.RequiredProperties = ["player_id", "currency", "country_code", "ip_address"];

/**
 * Player's ID in your system.
 * @member {String} player_id
 */
StartAMultiSessionRequest.prototype['player_id'] = undefined;

/**
 * Three-letter currency code (ISO 4217).
 * @member {String} currency
 */
StartAMultiSessionRequest.prototype['currency'] = undefined;

/**
 * Two-letter country code (ISO 3166-1 alpha-2).
 * @member {String} country_code
 */
StartAMultiSessionRequest.prototype['country_code'] = undefined;

/**
 * Player's IP address.
 * @member {String} ip_address
 */
StartAMultiSessionRequest.prototype['ip_address'] = undefined;

/**
 * optional Specific game IDs to include. If not provided, picks random games from each available provider.
 * @member {Array.<String>} game_ids
 */
StartAMultiSessionRequest.prototype['game_ids'] = undefined;

/**
 * optional Player's language preference (ISO 639-1).
 * @member {String} locale
 */
StartAMultiSessionRequest.prototype['locale'] = undefined;

/**
 * optional Device type: desktop, mobile, tablet.
 * @member {String} device
 */
StartAMultiSessionRequest.prototype['device'] = undefined;






export default StartAMultiSessionRequest;

