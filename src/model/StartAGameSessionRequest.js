/**
 * Game Aggregator API Documentation
 * Universal API for casino operators to access games from multiple providers through a single integration.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Numeric from './Numeric';

/**
 * The StartAGameSessionRequest model module.
 * @module model/StartAGameSessionRequest
 * @version 1.0.0
 */
class StartAGameSessionRequest {
    /**
     * Constructs a new <code>StartAGameSessionRequest</code>.
     * @alias module:model/StartAGameSessionRequest
     * @param gameId {Number} The ID of the game to start.
     * @param playerId {String} The player's ID in your system.
     * @param currency {String} Three-letter currency code (ISO 4217).
     * @param ipAddress {String} The player's IP address.
     * @param countryCode {String} Two-letter country code (ISO 3166-1 alpha-2).
     */
    constructor(gameId, playerId, currency, ipAddress, countryCode) { 
        
        StartAGameSessionRequest.initialize(this, gameId, playerId, currency, ipAddress, countryCode);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, gameId, playerId, currency, ipAddress, countryCode) { 
        obj['game_id'] = gameId;
        obj['player_id'] = playerId;
        obj['currency'] = currency;
        obj['ip_address'] = ipAddress;
        obj['country_code'] = countryCode;
    }

    /**
     * Constructs a <code>StartAGameSessionRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/StartAGameSessionRequest} obj Optional instance to populate.
     * @return {module:model/StartAGameSessionRequest} The populated <code>StartAGameSessionRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new StartAGameSessionRequest();

            if (data.hasOwnProperty('game_id')) {
                obj['game_id'] = ApiClient.convertToType(data['game_id'], 'Number');
            }
            if (data.hasOwnProperty('player_id')) {
                obj['player_id'] = ApiClient.convertToType(data['player_id'], 'String');
            }
            if (data.hasOwnProperty('currency')) {
                obj['currency'] = ApiClient.convertToType(data['currency'], 'String');
            }
            if (data.hasOwnProperty('ip_address')) {
                obj['ip_address'] = ApiClient.convertToType(data['ip_address'], 'String');
            }
            if (data.hasOwnProperty('country_code')) {
                obj['country_code'] = ApiClient.convertToType(data['country_code'], 'String');
            }
            if (data.hasOwnProperty('return_url')) {
                obj['return_url'] = ApiClient.convertToType(data['return_url'], 'String');
            }
            if (data.hasOwnProperty('locale')) {
                obj['locale'] = ApiClient.convertToType(data['locale'], 'String');
            }
            if (data.hasOwnProperty('provider')) {
                obj['provider'] = ApiClient.convertToType(data['provider'], 'String');
            }
            if (data.hasOwnProperty('freespin_id')) {
                obj['freespin_id'] = ApiClient.convertToType(data['freespin_id'], 'String');
            }
            if (data.hasOwnProperty('freespin_count')) {
                obj['freespin_count'] = ApiClient.convertToType(data['freespin_count'], 'Number');
            }
            if (data.hasOwnProperty('freespin_bet_amount')) {
                obj['freespin_bet_amount'] = ApiClient.convertToType(data['freespin_bet_amount'], Numeric);
            }
            if (data.hasOwnProperty('expire_days')) {
                obj['expire_days'] = ApiClient.convertToType(data['expire_days'], 'Number');
            }
            if (data.hasOwnProperty('device')) {
                obj['device'] = ApiClient.convertToType(data['device'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>StartAGameSessionRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>StartAGameSessionRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of StartAGameSessionRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['player_id'] && !(typeof data['player_id'] === 'string' || data['player_id'] instanceof String)) {
            throw new Error("Expected the field `player_id` to be a primitive type in the JSON string but got " + data['player_id']);
        }
        // ensure the json data is a string
        if (data['currency'] && !(typeof data['currency'] === 'string' || data['currency'] instanceof String)) {
            throw new Error("Expected the field `currency` to be a primitive type in the JSON string but got " + data['currency']);
        }
        // ensure the json data is a string
        if (data['ip_address'] && !(typeof data['ip_address'] === 'string' || data['ip_address'] instanceof String)) {
            throw new Error("Expected the field `ip_address` to be a primitive type in the JSON string but got " + data['ip_address']);
        }
        // ensure the json data is a string
        if (data['country_code'] && !(typeof data['country_code'] === 'string' || data['country_code'] instanceof String)) {
            throw new Error("Expected the field `country_code` to be a primitive type in the JSON string but got " + data['country_code']);
        }
        // ensure the json data is a string
        if (data['return_url'] && !(typeof data['return_url'] === 'string' || data['return_url'] instanceof String)) {
            throw new Error("Expected the field `return_url` to be a primitive type in the JSON string but got " + data['return_url']);
        }
        // ensure the json data is a string
        if (data['locale'] && !(typeof data['locale'] === 'string' || data['locale'] instanceof String)) {
            throw new Error("Expected the field `locale` to be a primitive type in the JSON string but got " + data['locale']);
        }
        // ensure the json data is a string
        if (data['provider'] && !(typeof data['provider'] === 'string' || data['provider'] instanceof String)) {
            throw new Error("Expected the field `provider` to be a primitive type in the JSON string but got " + data['provider']);
        }
        // ensure the json data is a string
        if (data['freespin_id'] && !(typeof data['freespin_id'] === 'string' || data['freespin_id'] instanceof String)) {
            throw new Error("Expected the field `freespin_id` to be a primitive type in the JSON string but got " + data['freespin_id']);
        }
        // ensure the json data is a string
        if (data['device'] && !(typeof data['device'] === 'string' || data['device'] instanceof String)) {
            throw new Error("Expected the field `device` to be a primitive type in the JSON string but got " + data['device']);
        }

        return true;
    }


}

StartAGameSessionRequest.RequiredProperties = ["game_id", "player_id", "currency", "ip_address", "country_code"];

/**
 * The ID of the game to start.
 * @member {Number} game_id
 */
StartAGameSessionRequest.prototype['game_id'] = undefined;

/**
 * The player's ID in your system.
 * @member {String} player_id
 */
StartAGameSessionRequest.prototype['player_id'] = undefined;

/**
 * Three-letter currency code (ISO 4217).
 * @member {String} currency
 */
StartAGameSessionRequest.prototype['currency'] = undefined;

/**
 * The player's IP address.
 * @member {String} ip_address
 */
StartAGameSessionRequest.prototype['ip_address'] = undefined;

/**
 * Two-letter country code (ISO 3166-1 alpha-2).
 * @member {String} country_code
 */
StartAGameSessionRequest.prototype['country_code'] = undefined;

/**
 * optional URL to redirect the player after game ends.
 * @member {String} return_url
 */
StartAGameSessionRequest.prototype['return_url'] = undefined;

/**
 * optional Player's language preference (ISO 639-1).
 * @member {String} locale
 */
StartAGameSessionRequest.prototype['locale'] = undefined;

/**
 * optional Provider slug to use for this session. Requires can_override_provider enabled.
 * @member {String} provider
 */
StartAGameSessionRequest.prototype['provider'] = undefined;

/**
 * optional Free spin bonus ID.
 * @member {String} freespin_id
 */
StartAGameSessionRequest.prototype['freespin_id'] = undefined;

/**
 * optional Number of free spins.
 * @member {Number} freespin_count
 */
StartAGameSessionRequest.prototype['freespin_count'] = undefined;

/**
 * optional Bet amount per free spin.
 * @member {module:model/Numeric} freespin_bet_amount
 */
StartAGameSessionRequest.prototype['freespin_bet_amount'] = undefined;

/**
 * optional Free spin expiration in days (1-30).
 * @member {Number} expire_days
 */
StartAGameSessionRequest.prototype['expire_days'] = undefined;

/**
 * optional Device type (desktop, mobile).
 * @member {String} device
 */
StartAGameSessionRequest.prototype['device'] = undefined;






export default StartAGameSessionRequest;

