/**
 * Game Aggregator API Documentation
 * Universal API for casino operators to access games from multiple providers through a single integration.
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import StartAMultiSession201ResponseDataGamesInner from './StartAMultiSession201ResponseDataGamesInner';

/**
 * The StartAMultiSession201ResponseData model module.
 * @module model/StartAMultiSession201ResponseData
 * @version 1.0.0
 */
class StartAMultiSession201ResponseData {
    /**
     * Constructs a new <code>StartAMultiSession201ResponseData</code>.
     * @alias module:model/StartAMultiSession201ResponseData
     */
    constructor() { 
        
        StartAMultiSession201ResponseData.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>StartAMultiSession201ResponseData</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/StartAMultiSession201ResponseData} obj Optional instance to populate.
     * @return {module:model/StartAMultiSession201ResponseData} The populated <code>StartAMultiSession201ResponseData</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new StartAMultiSession201ResponseData();

            if (data.hasOwnProperty('multi_session_id')) {
                obj['multi_session_id'] = ApiClient.convertToType(data['multi_session_id'], 'String');
            }
            if (data.hasOwnProperty('swipe_url')) {
                obj['swipe_url'] = ApiClient.convertToType(data['swipe_url'], 'String');
            }
            if (data.hasOwnProperty('total_games')) {
                obj['total_games'] = ApiClient.convertToType(data['total_games'], 'Number');
            }
            if (data.hasOwnProperty('games')) {
                obj['games'] = ApiClient.convertToType(data['games'], [StartAMultiSession201ResponseDataGamesInner]);
            }
            if (data.hasOwnProperty('expires_at')) {
                obj['expires_at'] = ApiClient.convertToType(data['expires_at'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>StartAMultiSession201ResponseData</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>StartAMultiSession201ResponseData</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['multi_session_id'] && !(typeof data['multi_session_id'] === 'string' || data['multi_session_id'] instanceof String)) {
            throw new Error("Expected the field `multi_session_id` to be a primitive type in the JSON string but got " + data['multi_session_id']);
        }
        // ensure the json data is a string
        if (data['swipe_url'] && !(typeof data['swipe_url'] === 'string' || data['swipe_url'] instanceof String)) {
            throw new Error("Expected the field `swipe_url` to be a primitive type in the JSON string but got " + data['swipe_url']);
        }
        if (data['games']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['games'])) {
                throw new Error("Expected the field `games` to be an array in the JSON data but got " + data['games']);
            }
            // validate the optional field `games` (array)
            for (const item of data['games']) {
                StartAMultiSession201ResponseDataGamesInner.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['expires_at'] && !(typeof data['expires_at'] === 'string' || data['expires_at'] instanceof String)) {
            throw new Error("Expected the field `expires_at` to be a primitive type in the JSON string but got " + data['expires_at']);
        }

        return true;
    }


}



/**
 * @member {String} multi_session_id
 */
StartAMultiSession201ResponseData.prototype['multi_session_id'] = undefined;

/**
 * @member {String} swipe_url
 */
StartAMultiSession201ResponseData.prototype['swipe_url'] = undefined;

/**
 * @member {Number} total_games
 */
StartAMultiSession201ResponseData.prototype['total_games'] = undefined;

/**
 * @member {Array.<module:model/StartAMultiSession201ResponseDataGamesInner>} games
 */
StartAMultiSession201ResponseData.prototype['games'] = undefined;

/**
 * @member {String} expires_at
 */
StartAMultiSession201ResponseData.prototype['expires_at'] = undefined;






export default StartAMultiSession201ResponseData;

